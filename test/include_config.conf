
FileSet {
  Name = "importantly"
  Include {
    #this only test parser,
    #for bacula this config will be incorrect because she isn't support relative path
    @./conf.d/importantly.list
    Exclude Dir Containing = .excludeme
  }
}

@./conf.d/client1.conf
